#include <iostream>
#include <string>
using namespace std;
int main()
{
    setlocale(LC_ALL, "rus");

    double c, b, a;
    
    cout << "Решить уравнение a^2+bx+c = 0\n";
    
    cout << "\n" << "c = ";
    
    cin >> c;
    
    cout << "\n" << "b = ";

    cin >> b;

    cout << "\n" << "a = ";

    cin >> a;
    if (a == 0) {
        if ((b == 0) && (c != 0))
        {
            cout << "\nНет решений";
        }
        else if (b == 0)
        {
            cout << "\nБесконечное количество решений";
        }
        else
        {
            cout << (-c) / b;
        }
    }
    else {
    
        if (b * b - 4 * a * c < 0) {
            cout << "Нет решений";
        }
        else if (b * b - 4 * a * c == 0) {
        
            cout << "Решение:"<<(-b)/(2*a);
        
        }
        else {
            cout << "Решения:" << ((-b) + sqrt(b * b - 4 * a * c)) / (2 * a) << "; " << ((-b) - sqrt(b * b - 4 * a * c)) / (2 * a);

        }
    
    }
}
